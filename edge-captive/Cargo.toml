[package]
name = "edge-captive"
version = "0.1.0"
edition = "2021"

[features]
std = ["domain/std"]

[dependencies]
log = { workspace = true }
domain = { version = "0.9.3", default-features = false, features = ["heapless"] }
heapless = { workspace = true }
