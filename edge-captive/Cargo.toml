[package]
name = "edge-captive"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[features]
default = ["io"]
io = [] # TODO
std = ["domain/std"]

[dependencies]
log = { workspace = true }
domain = { workspace = true }
octseq = { workspace = true }
