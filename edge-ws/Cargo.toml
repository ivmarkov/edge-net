[package]
name = "edge-ws"
version = "0.1.0"
edition = "2021"

[features]
nightly = ["embedded-io-async", "embedded-svc?/nightly"]

[dependencies]
embedded-io-async = { workspace = true, optional = true }
embedded-svc = { workspace = true, optional = true, default-features = false }
